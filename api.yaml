name: customers

type: snowflake

snowflake:
  query: |
    SELECT
      C_CUSTKEY
      ,C_NAME
      ,C_ADDRESS
      ,C_NATIONKEY
      ,C_PHONE
      ,C_ACCTBAL
      ,C_MKTSEGMENT
      ,C_COMMENT
    FROM
      SNOWFLAKE_SAMPLE_DATA.TPCH_SF1.CUSTOMER
    WHERE
      C_CUSTKEY = :custkey:
